{
  "name": "miniflare-fetch-bug",
  "type": "module",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "module": "dist/worker.mjs",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && node esbuild.mjs",
    "test": "npm run build && NODE_OPTIONS=\"--experimental-vm-modules --no-warnings\" vitest run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "itty-router": "^2.6.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.16.0",
    "esbuild": "^0.15.7",
    "miniflare": "2.8.2",
    "typescript": "^4.8.3",
    "vitest": "^0.23.2"
  }
}
